# Application
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fuse_finance

# API Configuration (placeholder - to be updated with actual vendor API key)
VENDOR_API_KEY=your_api_key_here
VENDOR_API_URL=https://api.challenge.fusefinance.com
# Optional: PgAdmin Configuration (if needed)
PGADMIN_EMAIL=admin@fuse.finance
PGADMIN_PASSWORD=admin

# --- Nodemailer/Email Delivery ---
SMTP_HOST=smtp4dev  # Use 'smtp4dev' if app runs in Docker Compose, 'localhost' if running outside
SMTP_PORT=25        # Use 25 if in Docker Compose, 2525 if connecting from host
SMTP_USER=
SMTP_PASS=
SMTP_FROM=reports@example.com
REPORT_RECIPIENTS=your@email.com